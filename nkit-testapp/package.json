{
  "name": "nkit-testapp",
  "version": "0.1.0",
  "private": true,
  "description": "nkit gesture/sensor testbed — camera feed + skeleton overlay + hover/grab/click test button + live threshold sliders",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^43.0.0"
  }
}
